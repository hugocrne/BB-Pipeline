#include <iostream>
#include <string>

#include "./include/types/scope.hpp"

int main() {
    Scope::Scope scope("data/scope.csv");

    // std::cout << "Scope: " << scope << std::endl;

    return 0;
}
